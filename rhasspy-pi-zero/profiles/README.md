# Profiles directory

This directory will contain the Rhasspy profile configuration.

When you run `install_pi_zero_docker.sh`, it will copy `profile.json` to `profiles/sv/profile.json` if it doesn't already exist.

You can edit `profiles/sv/profile.json` to customize your Rhasspy configuration before starting the Docker container.
